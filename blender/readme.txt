Animation and image renders from my blender project

Unfortunately due to file size restrictions cannot upload full video animations only a single or few frames.

For the dodgeball animations, a sports hall was modelled but majority of animations use a simpler background to improve render times. 
